// Notes for UI Updates

# Cmds
- npm run start:dev // to start local server
- netlify deploy --prod --dir=dist // to push to netlify
- lsof -i :9000 // open instance
- kill - 9 # // kill the deploy
- git push origin main → updates your public prototype
- git push gitlab main → updates your internal version

# Notes
- Migration Assistant Wizrd
-- Step 1: Remove from all option? 
-- Step 4: Change migrate button to the dropdown button.

# Issues
- not syncing after logged in
- local mode needs the ability to delete Joel comments